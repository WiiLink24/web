<div class="promptholder">
  <span style="height:100%; max-width:1500px;">
  <div class="images-container">
    <div class="images-wrapper">
      <div class="image-box">
        <img src="/img/acc-bg-1.webp" class="scroll-down" alt="Background 1" />
        <img src="/img/acc-bg-1.webp" class="scroll-down" alt="Background 1" />
      </div>
      <div class="image-box">
        <img src="/img/acc-bg-2.webp" class="scroll-up" alt="Background 2" />
        <img src="/img/acc-bg-2.webp" class="scroll-up" alt="Background 2" />
      </div>
      <div class="image-box">
        <img src="/img/acc-bg-3.webp" class="scroll-down" alt="Background 3" />
        <img src="/img/acc-bg-3.webp" class="scroll-down" alt="Background 3" />
      </div>
    </div>
  </div>

  <div class="innertext">
    <img 
      src="https://accounts.wiilink.ca/assets/img/account-logo.png" 
      alt="WiiLink24 Logo" 
      class="account-logo"
      loading="lazy"
    />
  <h1>
    Get your WiiLink Account!
  </h1>
  <h2>Everything's more fun with a WiiLink Account, get access to all our services and personalized data by creating one now!</h2>
  <a href="https://accounts.wiilink.ca" class="btn account-btn">
    <span class="user" style="margin-right: 5px"></span> 
    Access your Account
  </a>
</span>
</div>
</div>

<div
  class="bottomHelper"
  style="top:100%; width:100%; height:20px; position:relative;">
</div>

<style>
  .promptholder {
    top: 480px;
    left: 50%;
    transform: translate(-50%, 0);
    width: calc(100% - 20px);
    max-width: 1500px;
    height:600px;
    background: var(--bg-color-secondary);
    border: 2px solid #ffffff05;
    border-radius: 18px;
    overflow: hidden;
    z-index: 1;
    position: relative;
  }

  h1 {
    max-width: 600px;
    font-size: 3.5rem;
    font-family: Gilroy;
    font-weight: 700;
    line-height: 1;
    pointer-events: none;
    text-align: left;
    z-index: 10000;
    transition: 0.5s;
    position: relative;
  }

  button {
    color: white !important;
  }

  .innertext {
    width: 100%;
    height:100%;
    background: linear-gradient(to right, var(--bg-color-secondary) 20%, transparent 100%);
    display:flex;
    flex-direction: column;
    align-items: start;
    justify-content: center;
    padding-left:60px;
    gap:18px;
    z-index: 100;
    position: relative;
  }

  h2 {
    max-width: 800px;
    text-align: left;
    transition: 0.5s;
    pointer-events: none;
    margin-bottom: 20px;
    z-index: 1;
    position: relative;
  }

  .color {
    background: url("/img/header_bg.webp");
    background: AccentColor;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    filter: brightness(150%);
  }

  .images-container {
    top:-20px;
    left:50%;
    transform: translate(-50%, 0);
    width: 120%;
    min-width: 1000px;
    max-width: 2000px;
    height:100%;
    perspective: 1000px;
    opacity:0.6;
    padding: 20px;
    overflow: hidden;
    position:absolute;
  }

  .images-wrapper {
    height:100%;
    transform: rotateX(20deg);
    filter: blur(4px);
    transform-style: preserve-3d;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
    position: relative;
  }

  .image-box {
    height: 100%;
    border-radius: 20px;
    overflow: hidden;
    box-shadow: 0 10px 30px rgba(0,0,0,0.15);
    position: relative;
  }

  .image-box::before,
  .image-box::after {
    content: '';
    position: absolute;
    left: 0;
    width: 100%;
    height: 300px;
    z-index: 2;
    pointer-events: none;
  }

  .image-box::before {
    top: 0;
    background: linear-gradient(to bottom, 
      var(--bg-color-secondary) 0%,
      rgba(0,0,0,0) 100%
    );
  }

  .image-box::after {
    bottom: 0;
    background: linear-gradient(to top, 
      var(--bg-color-secondary) 0%,
      rgba(0,0,0,0) 100%
    );
  }

  .image-box img {
    width: 100%;
    object-fit: cover;
    will-change: transform;
    position: relative;
    z-index: 1;
  }

  .scroll-down {
    animation: scrollDown 15s linear infinite;
  }

  .scroll-up {
    animation: scrollUp 15s linear infinite;
  }

  @keyframes scrollDown {
    from { transform: translateY(-100%); }
    to { transform: translateY(0%); }
  }

  @keyframes scrollUp {
    from { transform: translateY(0%); }
    to { transform: translateY(-100%); }
  }

  .account-logo {
    width: 200px;
    filter: invert(var(--invert));
  }

  .account-btn {
    background-color: var(--bg-opaque-1);
    padding: 12px 18px;
    font-size: 18px;
    color: var(--color) !important;
    transition: 0.2s;
  }

  .account-btn:hover {
    background-color: var(--accent-color, AccentColor);
    color: white !important;
  }

  @media screen and (max-width: 1000px) {
    .promptholder {
      width: calc(100% - 20px);
    }
    .innertext {
      padding-left: 20px;
      padding-right: 20px;
    }
    .bottomHelper {
      height:160px !important;
    }
  }
</style>
